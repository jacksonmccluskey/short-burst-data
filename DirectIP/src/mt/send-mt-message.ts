export const sendMTMessage = async (buffer: Buffer) => {
	console.log('Sending MT Message...');
	console.log(buffer);
};
// const socket = net.createConnection(
// 	{ port: socketPort, host: socketHost },
